# QR Service Configuration
# Copy to .env and customize

# SQLite database path (default: qr_service.db in working directory)
DATABASE_PATH=qr_service.db

# Rocket configuration
ROCKET_ADDRESS=127.0.0.1
ROCKET_PORT=8000
ROCKET_LOG_LEVEL=normal

# Frontend static files directory (default: ../frontend/dist)
# Set to absolute path in production/Docker
# STATIC_DIR=../frontend/dist

# Base URL for short URL generation (tracked QR codes)
# BASE_URL=https://qr.example.com

# Rate limit window in seconds (default: 60)
# RATE_LIMIT_WINDOW_SECS=60
