services:
  qr-service:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - qr-data:/app/data
    environment:
      - DATABASE_PATH=/app/data/qr_service.db
      - ROCKET_ADDRESS=0.0.0.0
      - ROCKET_PORT=8000
    restart: unless-stopped

volumes:
  qr-data:
